import { jest } from "@jest/globals";

beforeEach(() => {
    jest.clearAllMocks();
    jest.resetAllMocks();
});

describe("POST /sign-up", () => {
    it("should create new user", () => {
        
    });
});